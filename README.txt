O arquivo .bd3 é o arquivo do banco de dados usado no Pomodoro.

Ele fica na pasta de build do projeto. Como não há necessidade da mesma aqui, mandei o arquivo junto com os código, tendo em vista que o mesmo não é criado com a aplicação.
